// taskForm.addEventListener('submit', function(e) {  // add an eventListener on button click
// 	e.preventDefault();
// 	let newTask = {};

// 	newTask.name = document.querySelector("#taskNameInput").value; // gets task name input
// 	newTask.due = document.querySelector("#duedate").value; // gets due date input
// 	newTask.priority = document.querySelector("#p-measure").value; // gets completion time input
// 	newTask.estimate = document.querySelector("#completetime").value; // gets completion time input
// 	newTask.completed = false; // sets completed to false

// 	let selectedProjectElem = document.querySelector('.projcheckbox:checked');
// 	let projectID = selectedProjectElem.id;
// 	console.log(projectID);
//     console.log(tasks);

// 	addNewTask(projectID, newTask);

// 	// let formValid = false;

// 	// if (newTask.name.value !== '' && selectedProjectElem !== 'underfined') {
// 	// 	formValid = true;
// 	// } else {
// 	// 	formValid = false;
// 	// 	console.log ('cannot submit task');
// 	// }

// 	// if (formValid) {
// 	// 	console.log('fields are filled out');
// 	// 	addNewTask(projectID, newTask);
// 	// }
// });

/* task is in the format:
	{
		"id": 1652870145971,
		"name": "taskNameInput.value", // this is equal to "project 1 task 1"
		"due": "dueDateInput.value",
		"priority": "prioritySelected.value",
		"estimate": "timeEstimated.value",
		"completed": false,
	}
*/

// function addNewTask(projectID, task) {
// 	let matchedProject = projects.find(project => project.id === projectID);
// 	matchedProject.tasks.push(task);

// 	console.log('new task added in relavent project');
// }